{
	"name": "instaAssignment",
	"version": "1.0.0",
	"packageManager": "pnpm@10.20.0",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"setup": "pnpm i && cd packages/ramsBackend && pnpm localDB:reset",
		"run:backend": "cd packages/ramsBackend && pnpm dev",
		"run:frontend": "cd packages/ramsClient && pnpm dev",
		"TypeCheck": "pnpm --dir packages/ramsBackend exec tsc --noEmit --pretty && pnpm --dir packages/ramsClient exec tsc --noEmit --pretty"
	},
	"devDependencies": {
		"prettier": "^3.6.2"
	}
}
